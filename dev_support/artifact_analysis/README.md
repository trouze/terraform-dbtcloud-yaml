# Artifact Analysis Folder

Use this folder for run/debug artifacts downloaded during migration web app testing.

Recommended structure:

- `run_<run_id>_<YYYYMMDD_HHMMSS>/`

Preferred helper:

- `./.cursor/skills/migration-webapp-browsermcp/scripts/store_run_artifacts.sh`

Keep sensitive material out of git history when applicable.
