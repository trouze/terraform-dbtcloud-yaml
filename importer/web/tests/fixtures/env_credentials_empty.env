# Test fixture: Empty baseline .env
# No environment credentials configured yet
#
# This file represents the initial state before any credentials are saved.

# Source dbt Cloud credentials (example)
DBT_SOURCE_HOST_URL='https://cloud.getdbt.com'
DBT_SOURCE_ACCOUNT_ID='12345'
DBT_SOURCE_API_TOKEN='dbtc_example_token'

# Target dbt Cloud credentials (example)
DBT_TARGET_HOST_URL='https://cloud.getdbt.com'
DBT_TARGET_ACCOUNT_ID='67890'
DBT_TARGET_API_TOKEN='dbtc_target_token'

# No DBT_ENV_CRED_* variables - credentials not yet configured
